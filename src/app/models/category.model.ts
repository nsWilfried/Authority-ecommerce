export interface Category {

    id: number, 
    name:string, 
    slug: string, 
    count:number;
    image: {
        src: string,
        alt: string
 
    }
       
    description: string
    
}
